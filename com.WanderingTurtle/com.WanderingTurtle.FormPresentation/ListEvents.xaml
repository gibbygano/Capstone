<UserControl x:Class="com.WanderingTurtle.FormPresentation.ListEvents"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="900">
    <Grid Background="#FFE5E5E5">
        <ListView x:Name="lvEmployeesList" HorizontalAlignment="Left" Height="350" VerticalAlignment="Top" Width="855" Margin="9,5,0,0">
            <ListView x:Name="lvEvents" HorizontalAlignment="Left" Height="419" Margin="10,10,0,0" VerticalAlignment="Top" Width="850" SelectionChanged="ListView_SelectionChanged">
                <ListView.View>
                    <GridView>
                        <!-- Here we bind the names that are held within the Event objects to the fields. This will allow us to present an
                    object to the listview and have it auto-populate with the objects data. Notice that the binding names are IDENTICAL
                    to the datafields within our Event object.-->
                        <GridViewColumn Width="140" Header="Event Name" DisplayMemberBinding="{Binding EventItemName}" />
                        <GridViewColumn Width="100" Header="Event Start Date" DisplayMemberBinding="{Binding EventStartDate}" />
                        <GridViewColumn Width="100" Header="Event End Date" DisplayMemberBinding="{Binding EventEndDate}" />
                        <GridViewColumn Width="60" Header="Min" DisplayMemberBinding="{Binding MinNumGuests}" />
                        <GridViewColumn Width="60" Header=" Max" DisplayMemberBinding="{Binding MaxNumGuests}" />
                        <GridViewColumn Width="100" Header="Transportation" DisplayMemberBinding="{Binding transportation}" />
                        <GridViewColumn Width="100" Header="On-Site" DisplayMemberBinding="{Binding onSite}" />
                        <GridViewColumn Width="100" Header="Reserved" DisplayMemberBinding="{Binding CurrentNumGuests}" />
                    </GridView>
                </ListView.View>
                
            </ListView>
        </ListView>
        
        <Button x:Name="btnAddEvent" Content="Add Event" HorizontalAlignment="Left" Margin="22,370,0,0" VerticalAlignment="Top" Click="btnAddEvent_Click" />
        <Button x:Name="btnUpdateEvent" Content="Update Event" HorizontalAlignment="Left" Margin="116,370,0,0" VerticalAlignment="Top" />
        <Button x:Name="btnDeleteEvent" Content="Delete Event" HorizontalAlignment="Left" Margin="215,370,0,0" VerticalAlignment="Top" />
    </Grid>
</UserControl>
