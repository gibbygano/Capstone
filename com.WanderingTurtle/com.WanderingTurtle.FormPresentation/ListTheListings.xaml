<UserControl x:Class="com.WanderingTurtle.FormPresentation.ListTheListings"
             Style="{DynamicResource UserControlStyle}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ListView Grid.Row="0" x:Name="lvEvents" SelectionChanged="ListView_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width="140" Header="Event Name" DisplayMemberBinding="{Binding EventName}" />
                    <GridViewColumn Width="100" Header="Event Start Date" DisplayMemberBinding="{Binding StartDate}" />
                    <GridViewColumn Width="100" Header="Event End Date" DisplayMemberBinding="{Binding EndDate}" />
                    <GridViewColumn Width="60" Header="Seats" DisplayMemberBinding="{Binding QuantityOffered}" />
                </GridView>
            </ListView.View>
        </ListView>
        <WrapPanel Grid.Row="1">
            <Button Margin="{DynamicResource Button Margin}" Padding="{DynamicResource Button Padding}" x:Name="btnAddListing" Content="Add Listing" Click="btnAddListing_Click" />
        </WrapPanel>
    </Grid>
</UserControl>