<UserControl x:Class="com.WanderingTurtle.FormPresentation.ListTheEmployees"
             Style="{DynamicResource UserControlStyle}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:common="clr-namespace:com.WanderingTurtle.Common;assembly=com.WanderingTurtle.Common"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 mc:Ignorable="d">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="Auto" />
		</Grid.RowDefinitions>
		<ListView Grid.Row="0" x:Name="LvEmployeesList" d:DataContext="{d:DesignInstance common:Employee }">
			<ListView.View>
				<GridView>
					<GridViewColumn Width ="100" Header="Employee ID" DisplayMemberBinding="{Binding EmployeeId}" />
					<GridViewColumn Width ="100" Header="First Name" DisplayMemberBinding="{Binding FirstName}" />
					<GridViewColumn Width ="150" Header="Last Name" DisplayMemberBinding="{Binding LastName}" />
					<GridViewColumn Width ="50" Header="Level" DisplayMemberBinding="{Binding Level}" />
				</GridView>
			</ListView.View>
		</ListView>

		<WrapPanel Grid.Row="1">
			<Button Margin="{DynamicResource 'Button Spacing'}" Content="Add Employee" Click="btnAddEmployee_Click" />
			<Button Margin="{DynamicResource 'Button Spacing'}" Content="Update Employee" Click="btnUpdateEmployee_Click" />
		</WrapPanel>
	</Grid>
</UserControl>