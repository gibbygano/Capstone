<UserControl x:Class="com.WanderingTurtle.FormPresentation.ListTheEmployees"
             Style="{DynamicResource UserControlStyle}"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ListView Grid.Row="0" x:Name="lvEmployeesList" GridViewColumnHeader.Click="lvEmployeeListHeaderClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn Width ="100" Header="Employee ID" DisplayMemberBinding="{Binding EmployeeID}" />
                    <GridViewColumn Width ="100" Header="First Name" DisplayMemberBinding="{Binding FirstName}" />
                    <GridViewColumn Width ="150" Header="Last Name" DisplayMemberBinding="{Binding LastName}" />
                    <GridViewColumn Width ="50" Header="Level" DisplayMemberBinding="{Binding Level}" />
                </GridView>
            </ListView.View>
        </ListView>

        <WrapPanel Grid.Row="1">
            <Button Margin="{DynamicResource Button Margin}" Padding="{DynamicResource Button Padding}" x:Name="btnAddEmployee" Content="Add Employee" Click="btnAddEmployee_Click" />
            <Button Margin="{DynamicResource Button Margin}" Padding="{DynamicResource Button Padding}" x:Name="btnUpdateEmployee" Content="Update Employee" Click="btnUpdateEmployee_Click" />
        </WrapPanel>
    </Grid>
</UserControl>