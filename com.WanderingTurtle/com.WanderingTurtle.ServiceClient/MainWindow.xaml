<Window x:Class="com.WanderingTurtle.ServiceClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:common="clr-namespace:com.WanderingTurtle.Common;assembly=WanderingTurtle.Common"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="350" Width="525"
        WindowStartupLocation="CenterScreen" d:DataContext="{d:DesignInstance common:AccountingDetails }">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Menu Grid.Row="0" IsMainMenu="True">
            <Label Content="Start Date:" />
            <DatePicker x:Name="StartDate" />
            <Separator />
            <Label Content="End Date:" />
            <DatePicker x:Name="EndDate" />
            <Button Content="Submit" IsDefault="True" Click="Button_Click" />
        </Menu>
        <TabControl Grid.Row="1">
            <TabItem Header="Invoices">
                <TreeView x:Name="dataInvoices" />
            </TabItem>
            <TabItem Header="Supplier Listings">
                <TreeView x:Name="dataListings" />
            </TabItem>
        </TabControl>
    </Grid>
</Window>